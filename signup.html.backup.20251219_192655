<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TimeLink - 회원가입</title>
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #4a1e8a 0%, #1a237e 100%);
            --secondary-gradient: linear-gradient(135deg, #7b1fa2 0%, #311b92 100%);
            --dark-bg: #0a0a1a;
            --darker-bg: #050510;
            --card-bg: rgba(255, 255, 255, 0.05);
            --card-bg-hover: rgba(255, 255, 255, 0.08);
            --text-light: #ffffff;
            --text-lighter: #f0f0ff;
            --text-gray: #b8b8d1;
            --text-dark: #8a8aaa;
            --accent-purple: #7c4dff;
            --accent-blue: #536dfe;
            --accent-pink: #ff4081;
            --accent-cyan: #00e5ff;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.08);
            --glass-border-hover: rgba(124, 77, 255, 0.3);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Noto Sans KR', sans-serif;
            background-color: var(--dark-bg);
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 20px;
            background-image: 
                radial-gradient(circle at 20% 30%, rgba(74, 30, 138, 0.15) 0%, transparent 25%),
                radial-gradient(circle at 80% 70%, rgba(26, 35, 126, 0.15) 0%, transparent 25%),
                radial-gradient(circle at 40% 90%, rgba(123, 31, 162, 0.1) 0%, transparent 30%);
        }
        
        .signup-container {
            max-width: 500px;
            width: 100%;
            margin: 0 auto;
        }
        
        .signup-card {
            background: var(--card-bg);
            border: 1px solid var(--glass-border);
            border-radius: 25px;
            padding: 40px;
            backdrop-filter: blur(20px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .signup-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: var(--primary-gradient);
        }
        
        .logo-section {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            margin-bottom: 15px;
        }
        
        .logo-icon {
            width: 60px;
            height: 60px;
            border-radius: 15px;
            background: var(--primary-gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: white;
            box-shadow: 0 8px 20px rgba(74, 30, 138, 0.3);
        }
        
        .logo-text {
            font-size: 2.5rem;
            font-weight: 800;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        .welcome-text {
            color: var(--text-gray);
            font-size: 1.1rem;
            line-height: 1.6;
            text-align: center;
            margin-bottom: 30px;
        }
        
        .welcome-text strong {
            color: var(--text-light);
            font-weight: 600;
        }
        
        .highlight {
            color: var(--accent-cyan);
            font-weight: 600;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-label {
            color: var(--text-light);
            font-weight: 500;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .form-label i {
            color: var(--accent-purple);
        }
        
        .input-group {
            position: relative;
        }
        
        .form-control {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--glass-border);
            color: var(--text-light);
            border-radius: 12px;
            padding: 15px 45px 15px 20px;
            font-size: 1rem;
            transition: all 0.3s ease;
            width: 100%;
        }
        
        .form-control:focus {
            background: rgba(255, 255, 255, 0.08);
            border-color: var(--accent-purple);
            color: var(--text-light);
            box-shadow: 0 0 0 3px rgba(124, 77, 255, 0.1);
            outline: none;
        }
        
        .input-icon {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-gray);
            pointer-events: none;
        }
        
        .password-toggle {
            position: absolute;
            right: 45px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: var(--text-gray);
            cursor: pointer;
            font-size: 1.1rem;
            padding: 5px;
            z-index: 2;
        }
        
        .password-strength {
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 2px;
            margin-top: 10px;
            overflow: hidden;
        }
        
        .password-strength-bar {
            height: 100%;
            width: 0%;
            background: #f56565;
            transition: width 0.3s, background 0.3s;
        }
        
        .password-strength.weak .password-strength-bar {
            width: 33%;
            background: #f56565;
        }
        
        .password-strength.medium .password-strength-bar {
            width: 66%;
            background: #ed8936;
        }
        
        .password-strength.strong .password-strength-bar {
            width: 100%;
            background: #48bb78;
        }
        
        .error-message {
            color: var(--accent-pink);
            font-size: 0.9rem;
            margin-top: 8px;
            display: none;
            padding-left: 5px;
        }
        
        .terms {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--glass-border);
            border-radius: 12px;
            padding: 20px;
            margin: 30px 0;
        }
        
        .terms-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .terms-content {
            max-height: 200px;
            overflow-y: auto;
            color: var(--text-gray);
            font-size: 0.9rem;
            line-height: 1.6;
            margin-bottom: 15px;
            padding: 15px;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 8px;
        }
        
        .terms-checkbox {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .terms-checkbox input[type="checkbox"] {
            width: 18px;
            height: 18px;
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid var(--glass-border);
            border-radius: 4px;
            cursor: pointer;
        }
        
        .terms-checkbox input[type="checkbox"]:checked {
            background-color: var(--accent-purple);
            border-color: var(--accent-purple);
        }
        
        .terms-checkbox label {
            color: var(--text-gray);
            font-size: 0.95rem;
            cursor: pointer;
        }
        
        .terms-checkbox a {
            color: var(--accent-purple);
            text-decoration: none;
            font-weight: 500;
        }
        
        .terms-checkbox a:hover {
            text-decoration: underline;
        }
        
        .submit-btn {
            background: var(--primary-gradient);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 16px;
            font-size: 1.1rem;
            font-weight: 600;
            width: 100%;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin: 25px 0;
        }
        
        .submit-btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(74, 30, 138, 0.4);
        }
        
        .submit-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }
        
        .login-link {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .login-text {
            color: var(--text-gray);
            margin-bottom: 15px;
            font-size: 0.95rem;
        }
        
        .login-btn {
            background: transparent;
            border: 2px solid var(--accent-purple);
            color: var(--accent-purple);
            border-radius: 12px;
            padding: 14px 30px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }
        
        .login-btn:hover {
            background: rgba(124, 77, 255, 0.1);
            transform: translateY(-2px);
        }
        
        /* 승인번호 모달 */
        .verification-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        
        .verification-content {
            background: var(--card-bg);
            border: 1px solid var(--glass-border);
            border-radius: 25px;
            padding: 40px;
            max-width: 450px;
            width: 90%;
            position: relative;
            animation: modalSlideUp 0.3s ease;
        }
        
        @keyframes modalSlideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .verification-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .verification-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: var(--primary-gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            color: white;
            margin: 0 auto 20px;
        }
        
        .verification-code {
            background: rgba(124, 77, 255, 0.1);
            border: 2px dashed var(--accent-purple);
            border-radius: 12px;
            padding: 25px;
            text-align: center;
            margin: 25px 0;
        }
        
        .verification-code h3 {
            font-size: 3rem;
            letter-spacing: 10px;
            color: var(--accent-cyan);
            margin: 10px 0;
        }
        
        .verification-note {
            color: var(--text-gray);
            font-size: 0.9rem;
            line-height: 1.6;
            text-align: center;
            margin-top: 15px;
        }
        
        .verification-input {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin: 25px 0;
        }
        
        .verification-input input {
            width: 50px;
            height: 60px;
            text-align: center;
            font-size: 2rem;
            background: rgba(255, 255, 255, 0.05);
            border: 2px solid var(--glass-border);
            border-radius: 10px;
            color: var(--text-light);
            transition: all 0.3s ease;
        }
        
        .verification-input input:focus {
            border-color: var(--accent-purple);
            outline: none;
            transform: translateY(-2px);
        }
        
        .verification-btn {
            background: var(--primary-gradient);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 16px;
            font-size: 1.1rem;
            font-weight: 600;
            width: 100%;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }
        
        .verification-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(74, 30, 138, 0.4);
        }
        
        /* 성공 메시지 */
        .success-message {
            background: rgba(0, 200, 83, 0.1);
            border: 1px solid rgba(0, 200, 83, 0.3);
            color: #00c853;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            display: none;
            margin-top: 20px;
            animation: fadeIn 0.5s;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .bonus-badge {
            background: rgba(255, 193, 7, 0.1);
            border: 1px solid rgba(255, 193, 7, 0.3);
            color: #ffc107;
            padding: 10px 20px;
            border-radius: 20px;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            margin-top: 15px;
            font-weight: 600;
        }
        
        /* 반응형 */
        @media (max-width: 576px) {
            .signup-card {
                padding: 30px 20px;
            }
            
            .logo-text {
                font-size: 2rem;
            }
            
            .logo-icon {
                width: 50px;
                height: 50px;
                font-size: 1.8rem;
            }
            
            .verification-input {
                gap: 5px;
            }
            
            .verification-input input {
                width: 40px;
                height: 50px;
                font-size: 1.5rem;
            }
            
            .verification-code h3 {
                font-size: 2.5rem;
                letter-spacing: 8px;
            }
        }
        
        /* 로딩 스피너 */
        .spinner {
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: white;
            animation: spin 1s linear infinite;
            display: inline-block;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* 입력 힌트 */
        .input-hint {
            font-size: 0.8rem;
            color: var(--text-gray);
            margin-top: 5px;
            padding-left: 5px;
        }
    </style>
    <link rel="stylesheet" href="src/styles/common.css">
</head>
<body>
    <div class="signup-container">
        <div class="signup-card">
            <!-- 로고 섹션 -->
            <div class="logo-section">
                <div class="logo">
                    <div class="logo-icon">
                        <i class="bi bi-infinity"></i>
                    </div>
                    <div class="logo-text">TimeLink</div>
                </div>
                <p class="welcome-text">
                    회원가입하고 <span class="highlight">10,000 TL</span> 보너스를 받으세요!<br>
                    <small>이메일 인증 없이 바로 시작합니다</small>
                </p>
            </div>
            
            <!-- 회원가입 폼 -->
            <form id="signupForm">
                <!-- 이름 입력 -->
                <div class="form-group">
                    <label class="form-label">
                        <i class="bi bi-person"></i>
                        이름
                    </label>
                    <div class="input-group">
                        <input type="text" 
                               class="form-control" 
                               id="name" 
                               name="name" 
                               placeholder="이름을 입력하세요"
                               required>
                        <span class="input-icon">
                            <i class="bi bi-person-check"></i>
                        </span>
                    </div>
                    <div class="error-message" id="nameError">이름을 입력해주세요.</div>
                </div>
                
                <!-- 이메일 입력 -->
                <div class="form-group">
                    <label class="form-label">
                        <i class="bi bi-envelope"></i>
                        이메일 주소
                    </label>
                    <div class="input-group">
                        <input type="email" 
                               class="form-control" 
                               id="email" 
                               name="email" 
                               placeholder="example@email.com"
                               required>
                        <span class="input-icon">
                            <i class="bi bi-envelope-check"></i>
                        </span>
                    </div>
                    <div class="error-message" id="emailError">유효한 이메일 주소를 입력해주세요.</div>
                </div>
                
                <!-- 비밀번호 입력 -->
                <div class="form-group">
                    <label class="form-label">
                        <i class="bi bi-lock"></i>
                        비밀번호
                    </label>
                    <div class="input-group">
                        <input type="password" 
                               class="form-control" 
                               id="password" 
                               name="password" 
                               placeholder="최소 8자 이상"
                               required>
                        <button type="button" class="password-toggle" onclick="togglePassword('password')">
                            <i class="bi bi-eye"></i>
                        </button>
                        <span class="input-icon">
                            <i class="bi bi-key"></i>
                        </span>
                    </div>
                    <div class="password-strength" id="passwordStrength">
                        <div class="password-strength-bar"></div>
                    </div>
                    <div class="error-message" id="passwordError">비밀번호는 8자 이상이어야 합니다.</div>
                </div>
                
                <!-- 비밀번호 확인 -->
                <div class="form-group">
                    <label class="form-label">
                        <i class="bi bi-lock-fill"></i>
                        비밀번호 확인
                    </label>
                    <div class="input-group">
                        <input type="password" 
                               class="form-control" 
                               id="confirmPassword" 
                               name="confirmPassword" 
                               placeholder="비밀번호를 다시 입력하세요"
                               required>
                        <button type="button" class="password-toggle" onclick="togglePassword('confirmPassword')">
                            <i class="bi bi-eye"></i>
                        </button>
                        <span class="input-icon">
                            <i class="bi bi-shield-check"></i>
                        </span>
                    </div>
                    <div class="error-message" id="confirmPasswordError">비밀번호가 일치하지 않습니다.</div>
                </div>
                
                <!-- 이용약관 -->
                <div class="terms">
                    <div class="terms-header">
                        <i class="bi bi-file-text" style="color: var(--accent-purple);"></i>
                        <h4 style="color: var(--text-light); margin: 0;">이용약관 동의</h4>
                    </div>
                    
                    <div class="terms-content">
                        <p><strong>제1조 (목적)</strong></p>
                        <p>이 약관은 TimeLink 서비스 이용과 관련하여 필요한 사항을 규정합니다.</p>
                        <br>
                        <p><strong>제2조 (회원가입)</strong></p>
                        <p>회원가입 시 10,000 TL의 보너스를 지급합니다.</p>
                        <br>
                        <p><strong>제3조 (서비스 이용)</strong></p>
                        <p>음원 스트리밍 및 디지털 콘텐츠 제공을 주요 서비스로 합니다.</p>
                    </div>
                    
                    <div class="terms-checkbox">
                        <input type="checkbox" id="terms" name="terms" required>
                        <label for="terms">
                            <a href="#" onclick="showTerms()">이용약관</a> 및 
                            <a href="#" onclick="showPrivacy()">개인정보 처리방침</a>에 동의합니다
                        </label>
                    </div>
                </div>
                
                <!-- 회원가입 버튼 -->
                <button type="submit" class="submit-btn" id="submitBtn">
                    <i class="bi bi-person-plus"></i>
                    무료로 회원가입
                </button>
            </form>
            
            <!-- 로그인 링크 -->
            <div class="login-link">
                <p class="login-text">이미 TimeLink 계정이 있으신가요?</p>
                <a href="login.html" class="login-btn">
                    <i class="bi bi-box-arrow-in-right"></i>
                    로그인하기
                </a>
            </div>
            
            <!-- 성공 메시지 -->
            <div class="success-message" id="successMessage">
                <div style="font-size: 2rem; margin-bottom: 10px;">🎉</div>
                <h4 style="margin-bottom: 10px;">회원가입이 완료되었습니다!</h4>
                <div class="bonus-badge">
                    <i class="bi bi-gift-fill"></i>
                    10,000 TL 보너스 지급 완료!
                </div>
                <p style="margin-top: 15px; font-size: 0.9rem;">
                    지금 바로 음원 스트리밍 서비스를 이용해보세요.<br>
                    3초 후 자동으로 로그인 페이지로 이동합니다.
                </p>
            </div>
        </div>
    </div>
    
    <!-- 승인번호 모달 -->
    <div class="verification-modal" id="verificationModal">
        <div class="verification-content">
            <div class="verification-header">
                <div class="verification-icon">
                    <i class="bi bi-shield-check"></i>
                </div>
                <h3>승인번호 확인</h3>
                <p style="color: var(--text-gray);">아래 승인번호를 입력하여 회원가입을 완료하세요</p>
            </div>
            
            <div class="verification-code">
                <p style="color: var(--text-gray); font-size: 0.9rem;">회원가입 승인번호</p>
                <h3 id="verificationCode">000000</h3>
                <p style="color: var(--text-gray); font-size: 0.8rem;">(이 번호를 기억해주세요)</p>
            </div>
            
            <div class="verification-input">
                <input type="text" maxlength="1" oninput="moveToNext(this, 1)" autocomplete="off">
                <input type="text" maxlength="1" oninput="moveToNext(this, 2)" autocomplete="off">
                <input type="text" maxlength="1" oninput="moveToNext(this, 3)" autocomplete="off">
                <input type="text" maxlength="1" oninput="moveToNext(this, 4)" autocomplete="off">
                <input type="text" maxlength="1" oninput="moveToNext(this, 5)" autocomplete="off">
                <input type="text" maxlength="1" oninput="moveToNext(this, 6)" autocomplete="off">
            </div>
            
            <p class="verification-note">
                <i class="bi bi-info-circle"></i>
                승인번호 입력 후 자동으로 10,000 TL이 지급됩니다.<br>
                다음 로그인부터는 이메일과 비밀번호로 로그인할 수 있습니다.
            </p>
            
            <button type="button" class="verification-btn" onclick="verifyCode()">
                <i class="bi bi-check-circle"></i>
                확인 완료
            </button>
        </div>
    </div>

    <script>
        // API 설정
        const API_BASE_URL = 'https://timelink-backend.timelink-api.workers.dev';
        let generatedCode = '';
        let verificationTimer = null;
        
        // DOM 로드 시 이벤트 리스너 설정
        document.addEventListener('DOMContentLoaded', function() {
            console.log('TimeLink 회원가입 페이지 로드됨');
            console.log('API 기본 URL:', API_BASE_URL);
            
            document.getElementById('signupForm').addEventListener('submit', handleSignup);
            document.getElementById('password').addEventListener('input', updatePasswordStrength);
            document.getElementById('confirmPassword').addEventListener('input', validatePasswordMatch);
            
            // 모든 입력 필드에 엔터 키 이벤트 추가
            document.querySelectorAll('input').forEach(input => {
                input.addEventListener('keypress', function(event) {
                    if (event.key === 'Enter') {
                        event.preventDefault();
                        if (this.id !== 'confirmPassword') {
                            const nextInput = this.closest('.form-group').nextElementSibling?.querySelector('input');
                            if (nextInput) nextInput.focus();
                        } else {
                            handleSignup(event);
                        }
                    }
                });
            });
        });
        
        // 이메일 유효성 검사
        function validateEmail(email) {
            const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            return re.test(email);
        }
        
        // 비밀번호 강도 업데이트
        function updatePasswordStrength() {
            const password = document.getElementById('password').value;
            const strengthBar = document.getElementById('passwordStrength');
            const bar = strengthBar.querySelector('.password-strength-bar');
            
            // 비밀번호 강도 계산
            let strength = 0;
            if (password.length >= 8) strength++;
            if (/[A-Z]/.test(password)) strength++;
            if (/[0-9]/.test(password)) strength++;
            if (/[^A-Za-z0-9]/.test(password)) strength++;
            
            // UI 업데이트
            strengthBar.className = 'password-strength';
            if (password.length > 0) {
                if (strength < 2) {
                    strengthBar.classList.add('weak');
                } else if (strength < 4) {
                    strengthBar.classList.add('medium');
                } else {
                    strengthBar.classList.add('strong');
                }
            }
            
            // 에러 메시지 처리
            const passwordError = document.getElementById('passwordError');
            if (password.length > 0 && password.length < 8) {
                passwordError.style.display = 'block';
                passwordError.textContent = '비밀번호는 8자 이상이어야 합니다.';
            } else {
                passwordError.style.display = 'none';
            }
        }
        
        // 비밀번호 확인 검사
        function validatePasswordMatch() {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;
            const errorElement = document.getElementById('confirmPasswordError');
            
            if (confirmPassword.length > 0 && password !== confirmPassword) {
                errorElement.style.display = 'block';
                errorElement.textContent = '비밀번호가 일치하지 않습니다.';
                return false;
            } else {
                errorElement.style.display = 'none';
                return true;
            }
        }
        
        // 비밀번호 표시/숨기기 토글
        function togglePassword(inputId) {
            const passwordInput = document.getElementById(inputId);
            const toggleButton = passwordInput.parentElement.querySelector('.password-toggle i');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleButton.className = 'bi bi-eye-slash';
            } else {
                passwordInput.type = 'password';
                toggleButton.className = 'bi bi-eye';
            }
        }
        
        // 폼 유효성 검사
        function validateForm() {
            let isValid = true;
            
            // 이름 검사
            const name = document.getElementById('name').value.trim();
            const nameError = document.getElementById('nameError');
            if (!name) {
                nameError.style.display = 'block';
                isValid = false;
            } else {
                nameError.style.display = 'none';
            }
            
            // 이메일 검사
            const email = document.getElementById('email').value.trim();
            const emailError = document.getElementById('emailError');
            if (!email || !validateEmail(email)) {
                emailError.style.display = 'block';
                emailError.textContent = !email ? '이메일을 입력해주세요.' : '유효한 이메일 주소를 입력해주세요.';
                isValid = false;
            } else {
                emailError.style.display = 'none';
            }
            
            // 비밀번호 검사
            const password = document.getElementById('password').value;
            const passwordError = document.getElementById('passwordError');
            if (!password || password.length < 8) {
                passwordError.style.display = 'block';
                passwordError.textContent = !password ? '비밀번호를 입력해주세요.' : '비밀번호는 8자 이상이어야 합니다.';
                isValid = false;
            } else {
                passwordError.style.display = 'none';
            }
            
            // 비밀번호 확인 검사
            if (!validatePasswordMatch()) {
                isValid = false;
            }
            
            // 이용약관 동의 검사
            const terms = document.getElementById('terms').checked;
            if (!terms) {
                alert('이용약관에 동의해주세요.');
                isValid = false;
            }
            
            return isValid;
        }
        
        // 6자리 승인번호 생성
        function generateVerificationCode() {
            return Math.floor(100000 + Math.random() * 900000).toString();
        }
        
        // 승인번호 모달 표시
        function showVerificationModal(code) {
            generatedCode = code;
            document.getElementById('verificationCode').textContent = code;
            document.getElementById('verificationModal').style.display = 'flex';
            
            // 입력 필드 초기화 및 첫 번째 필드에 포커스
            const inputs = document.querySelectorAll('.verification-input input');
            inputs.forEach(input => input.value = '');
            inputs[0].focus();
            
            // 5분 후 모달 자동 닫기
            if (verificationTimer) clearTimeout(verificationTimer);
            verificationTimer = setTimeout(() => {
                hideVerificationModal();
                alert('입력 시간이 초과되었습니다. 다시 시도해주세요.');
            }, 5 * 60 * 1000); // 5분
        }
        
        // 승인번호 모달 숨기기
        function hideVerificationModal() {
            document.getElementById('verificationModal').style.display = 'none';
            if (verificationTimer) {
                clearTimeout(verificationTimer);
                verificationTimer = null;
            }
        }
        
        // 승인번호 입력 필드 이동
        function moveToNext(input, nextIndex) {
            const inputs = document.querySelectorAll('.verification-input input');
            
            // 현재 입력값이 있으면 다음 필드로 이동
            if (input.value.length === 1 && nextIndex < inputs.length) {
                inputs[nextIndex].focus();
            }
            
            // 모든 필드가 채워졌는지 확인
            checkVerificationComplete();
        }
        
        // 승인번호 입력 완료 확인
        function checkVerificationComplete() {
            const inputs = document.querySelectorAll('.verification-input input');
            let code = '';
            inputs.forEach(input => code += input.value);
            
            if (code.length === 6) {
                // 자동 확인
                if (code === generatedCode) {
                    verifyCode();
                } else {
                    // 흔들기 효과
                    const modal = document.querySelector('.verification-content');
                    modal.style.animation = 'none';
                    setTimeout(() => {
                        modal.style.animation = 'modalSlideUp 0.3s ease';
                        inputs.forEach(input => {
                            input.style.borderColor = 'var(--accent-pink)';
                        });
                        setTimeout(() => {
                            inputs.forEach(input => {
                                input.style.borderColor = '';
                            });
                        }, 1000);
                    }, 10);
                }
            }
        }
        
        // 승인번호 검증
        function verifyCode() {
            const inputs = document.querySelectorAll('.verification-input input');
            let enteredCode = '';
            inputs.forEach(input => enteredCode += input.value);
            
            if (enteredCode.length !== 6) {
                alert('6자리 승인번호를 모두 입력해주세요.');
                return;
            }
            
            if (enteredCode !== generatedCode) {
                alert('승인번호가 일치하지 않습니다. 다시 확인해주세요.');
                return;
            }
            
            // 성공 처리
            hideVerificationModal();
            
            // 회원가입 성공 메시지 표시
            document.getElementById('signupForm').style.display = 'none';
            const successMessage = document.getElementById('successMessage');
            successMessage.style.display = 'block';
            
            // 사용자 데이터 저장 (실제로는 서버에 저장되어야 함)
            const userData = {
                name: document.getElementById('name').value.trim(),
                email: document.getElementById('email').value.trim(),
                password: document.getElementById('password').value,
                balance: 10000, // 10,000 TL 보너스
                joined: new Date().toISOString(),
                verified: true
            };
            
            // 로컬 스토리지에 저장 (임시)
            localStorage.setItem('timelink_user', JSON.stringify(userData));
            localStorage.setItem('timelink_auth', 'true');
            
            console.log('회원가입 완료:', userData);
            
            // 3초 후 로그인 페이지로 리다이렉트
            setTimeout(() => {
                window.location.href = 'login.html';
            }, 3000);
        }
        
        // 회원가입 처리
        async function handleSignup(event) {
            event.preventDefault();
            
            if (!validateForm()) {
                return;
            }
            
            const submitBtn = document.getElementById('submitBtn');
            const originalText = submitBtn.innerHTML;
            
            try {
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<span class="spinner"></span> 처리 중...';
                
                // 실제 서버 API 호출 대신 임시 승인번호 생성
                // 실제 구현 시 아래 주석 처리된 API 호출을 사용
                
                /*
                // 실제 API 호출 (백엔드 구현 필요)
                const formData = {
                    name: document.getElementById('name').value.trim(),
                    email: document.getElementById('email').value.trim(),
                    password: document.getElementById('password').value
                };
                
                const response = await fetch(`${API_BASE_URL}/api/auth/signup`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });
                
                const data = await response.json();
                
                if (!response.ok) {
                    throw new Error(data.message || '회원가입에 실패했습니다.');
                }
                
                // 성공 시 승인번호 발급
                const verificationCode = generateVerificationCode();
                */
                
                // 임시: 승인번호 생성 및 모달 표시
                const verificationCode = generateVerificationCode();
                console.log('생성된 승인번호:', verificationCode);
                
                // 승인번호 모달 표시
                setTimeout(() => {
                    showVerificationModal(verificationCode);
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalText;
                }, 1000);
                
            } catch (error) {
                console.error('Signup error:', error);
                
                // 에러 메시지 표시
                if (error.message.includes('이미 등록된 이메일')) {
                    document.getElementById('emailError').textContent = error.message;
                    document.getElementById('emailError').style.display = 'block';
                } else {
                    alert(`회원가입 중 오류가 발생했습니다: ${error.message}`);
                }
                
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalText;
            }
        }
        
        // 약관 및 개인정보 처리방침 팝업
        function showTerms() {
            event.preventDefault();
            alert('이용약관 내용이 여기에 표시됩니다.\n\n실제 서비스에서는 별도의 페이지로 구현됩니다.');
        }
        
        function showPrivacy() {
            event.preventDefault();
            alert('개인정보 처리방침 내용이 여기에 표시됩니다.\n\n실제 서비스에서는 별도의 페이지로 구현됩니다.');
        }
    </script>
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <script src="src/script/common-components.js"></script>
</body>
</html>
