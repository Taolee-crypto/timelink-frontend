{
  "name": "timelink-frontend",
  "version": "1.0.0",
  "description": "TimeLink P2P Music Platform",
  "homepage": "https://timelink.digital",
  "scripts": {
    "dev": "npx live-server --host=127.0.0.1 --port=3000",
    "deploy": "npx gh-pages -d . -t true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.5"
  }
}
